/**
 * The single canonical wording for DRAIVA's customer-facing multilingual
 * policy. Reused wherever product/prompt copy states the rule, so every
 * surface (the customer-facing WhatsApp AI prompt, the internal staff
 * copilot prompt, etc.) agrees on the exact same sentence instead of
 * drifting into slightly different wording over time.
 */
export const DRAIVA_LANGUAGE_POLICY =
  "DRAIVA responds in the customer's language whenever it can reasonably determine the language.";
